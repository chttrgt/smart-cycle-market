{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "tsnd --respawn -r tsconfig-paths/register --transpile-only ./src/index.ts",
    "build": "tsc && tsc-alias",
    "start": "node ./dist"
  },
  "keywords": [],
  "author": "cihatturgut",
  "license": "ISC",
  "description": "This is the api built for smart-cycle-market app",
  "dependencies": {
    "express": "^4.21.2",
    "tsc-alias": "^1.8.10",
    "tsconfig-paths": "^4.2.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.7",
    "prettier": "^3.4.2",
    "ts-node-dev": "^2.0.0"
  }
}
